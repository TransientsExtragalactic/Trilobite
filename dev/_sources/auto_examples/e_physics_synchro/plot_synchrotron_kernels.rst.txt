
.. DO NOT EDIT.
.. THIS FILE WAS AUTOMATICALLY GENERATED BY SPHINX-GALLERY.
.. TO MAKE CHANGES, EDIT THE SOURCE PYTHON FILE:
.. "auto_examples/e_physics_synchro/plot_synchrotron_kernels.py"
.. LINE NUMBERS ARE GIVEN BELOW.

.. only:: html

    .. note::
        :class: sphx-glr-download-link-note

        :ref:`Go to the end <sphx_glr_download_auto_examples_e_physics_synchro_plot_synchrotron_kernels.py>`
        to download the full example code.

.. rst-class:: sphx-glr-example-title

.. _sphx_glr_auto_examples_e_physics_synchro_plot_synchrotron_kernels.py:


The Synchrotron Kernel Functions F(x) and G(x)
===============================================

The synchrotron emission spectrum of a single relativistic electron is governed
by two dimensionless kernel functions, :math:`F(x)` and :math:`G(x)`, where
:math:`x = \nu / \nu_c` is the ratio of the observing frequency to the
electron's critical synchrotron frequency :math:`\nu_c \propto \gamma^2 B`.

The first kernel :math:`F(x)` enters the total emitted power per unit frequency
for an unpolarized electron:

.. math::

    P(\nu) = \frac{\sqrt{3}\, e^3 B \sin\alpha}{m_e c^2}\, F\!\left(\frac{\nu}{\nu_c}\right),
    \qquad F(x) = x \int_x^\infty K_{5/3}(\xi)\, d\xi

The second kernel :math:`G(x)` is related to the polarized component:

.. math::

    G(x) = x\, K_{2/3}(x)

Both kernels have known analytic asymptotic limits that are important for
understanding synchrotron spectra:

- :math:`F(x) \approx \tfrac{4\pi}{\sqrt{3}\,\Gamma(1/3)} \left(\tfrac{x}{2}\right)^{1/3}` for
  :math:`x \ll 1`  (the optically thin low-frequency tail, :math:`\propto \nu^{1/3}`)
- :math:`F(x) \approx \sqrt{\tfrac{\pi x}{2}} e^{-x}` for :math:`x \gg 1`  (exponential cutoff)

This example demonstrates both the exact and interpolated kernels available in
Triceratops and validates that the interpolation is accurate for practical use.

For theoretical background see :ref:`synchrotron_theory`.

Relevant API References
-----------------------
- :func:`~radiation.synchrotron.core.first_synchrotron_kernel`
- :func:`~radiation.synchrotron.core.second_synchrotron_kernel`
- :func:`~radiation.synchrotron.core.get_first_kernel_interpolator`
- :func:`~radiation.synchrotron.core.get_second_kernel_interpolator`

.. GENERATED FROM PYTHON SOURCE LINES 43-57

.. code-block:: Python


    from time import perf_counter

    import matplotlib.pyplot as plt
    import numpy as np

    from triceratops.radiation.synchrotron.core import (
        first_synchrotron_kernel,
        get_first_kernel_interpolator,
        get_second_kernel_interpolator,
        second_synchrotron_kernel,
    )
    from triceratops.utils.plot_utils import set_plot_style








.. GENERATED FROM PYTHON SOURCE LINES 58-69

Kernel Setup
------------

We evaluate the kernels over a wide dynamic range in :math:`x`, from deep in the
low-frequency regime (:math:`x \ll 1`) through the exponential cutoff (:math:`x \gg 1`).

.. note::

    The exact kernel functions use modified Bessel functions (:math:`K_{5/3}` and
    :math:`K_{2/3}`) from :mod:`scipy.special` and are computed via numerical
    integration. This makes them accurate but slow for population-integral loops.

.. GENERATED FROM PYTHON SOURCE LINES 69-76

.. code-block:: Python


    x = np.geomspace(1e-4, 1e2, 1000)

    # Exact kernels — vectorized over x
    F_exact = first_synchrotron_kernel(x)
    G_exact = second_synchrotron_kernel(x)





.. rst-class:: sphx-glr-script-out

 .. code-block:: none

      0% |          | 🦖 0/1000 [00:00<?, ?it/s]     20% |█▉        | 🦖 195/1000 [00:00<00:00, 1942.32it/s]     48% |████▊     | 🦖 485/1000 [00:00<00:00, 2503.86it/s]    100% |██████████| 🦖 1000/1000 [00:00<00:00, 3898.05it/s]




.. GENERATED FROM PYTHON SOURCE LINES 77-92

Asymptotic Limits
-----------------

The two asymptotic forms of :math:`F(x)` provide physical intuition:

.. math::

    F(x) \approx \frac{4\pi}{\sqrt{3}\,\Gamma(1/3)}\,2^{-1/3}\,x^{1/3}
    \quad (x \ll 1),
    \qquad
    F(x) \approx \sqrt{\frac{\pi x}{2}}\,e^{-x}
    \quad (x \gg 1)

The low-:math:`x` limit directly produces the :math:`\nu^{1/3}` optically thin
synchrotron tail observed below the injection break in radio afterglow spectra.

.. GENERATED FROM PYTHON SOURCE LINES 92-101

.. code-block:: Python


    from scipy.special import gamma as gamma_func  # noqa: E402

    # Low-x asymptote: F(x) ≈ (4π / (sqrt(3) Γ(1/3))) * (x/2)^(1/3)
    F_low_x = (4 * np.pi / (np.sqrt(3) * gamma_func(1 / 3))) * (x / 2) ** (1 / 3)

    # High-x asymptote: F(x) ≈ sqrt(π x / 2) * exp(-x)
    F_high_x = np.sqrt(np.pi * x / 2) * np.exp(-x)








.. GENERATED FROM PYTHON SOURCE LINES 102-109

Plot: F(x) and G(x) with Asymptotic Limits
-------------------------------------------

Both kernels peak near :math:`x \approx 0.3`–:math:`0.5` and fall off
steeply for :math:`x \gg 1`. The ratio :math:`G(x)/F(x)` characterizes the
degree of linear polarization; for an isotropic electron pitch-angle
distribution, the net polarization vanishes.

.. GENERATED FROM PYTHON SOURCE LINES 109-133

.. code-block:: Python


    set_plot_style()

    fig, ax = plt.subplots(figsize=(8, 5))

    ax.loglog(x, F_exact, lw=2.0, label=r"$F(x)$ (exact)")
    ax.loglog(x, G_exact, lw=2.0, label=r"$G(x)$ (exact)")
    ax.loglog(x, F_low_x, ls="--", color="C0", alpha=0.7, label=r"$F(x) \approx (x/2)^{1/3}$ (low-$x$)")
    ax.loglog(x, F_high_x, ls=":", color="C0", alpha=0.7, label=r"$F(x) \approx \sqrt{\pi x/2}\,e^{-x}$ (high-$x$)")

    ax.axvline(1.0, color="gray", ls="--", alpha=0.5, lw=1)
    ax.text(1.2, 2e-1, r"$x = 1$", color="gray", fontsize=10)

    ax.set_xlabel(r"$x = \nu / \nu_c$")
    ax.set_ylabel(r"Kernel value")
    ax.set_title("Synchrotron Kernel Functions")
    ax.set_xlim(1e-4, 1e2)
    ax.set_ylim(1e-6, 3)
    ax.legend()
    ax.grid(True, which="both", ls="--", alpha=0.3)

    plt.tight_layout()
    plt.show()




.. image-sg:: /auto_examples/e_physics_synchro/images/sphx_glr_plot_synchrotron_kernels_001.png
   :alt: Synchrotron Kernel Functions
   :srcset: /auto_examples/e_physics_synchro/images/sphx_glr_plot_synchrotron_kernels_001.png
   :class: sphx-glr-single-img





.. GENERATED FROM PYTHON SOURCE LINES 134-149

Interpolated vs Exact F(x): Accuracy
-------------------------------------

For population-level spectra that integrate over the electron distribution,
the kernel is called inside an inner loop over Lorentz factor :math:`\gamma`.
Triceratops provides interpolated kernels via
:func:`~radiation.synchrotron.core.get_first_kernel_interpolator` that
pre-tabulate :math:`F(x)` on a log-space grid.

.. hint::

    Use the interpolated kernel whenever you call
    :func:`~radiation.synchrotron.core.compute_ME_spectrum_from_dist_function`
    repeatedly (e.g. in an MCMC loop). The speedup factor is typically
    :math:`10`–:math:`100\times` with negligible accuracy loss.

.. GENERATED FROM PYTHON SOURCE LINES 149-175

.. code-block:: Python


    t0 = perf_counter()
    kernel_interp = get_first_kernel_interpolator(x_min=1e-5, x_max=1e2, num_points=2000)
    t_setup = perf_counter() - t0

    # Interpolated values on the same x grid (excluding the out-of-range edges)
    F_interp = kernel_interp(x)
    F_ref = first_synchrotron_kernel(x)
    rel_error = np.abs(F_interp - F_ref) / np.where(F_ref > 0, F_ref, F_ref)

    # Timing comparison
    n_calls = 500
    t0 = perf_counter()
    first_synchrotron_kernel(x[:n_calls])
    t_exact = perf_counter() - t0

    t0 = perf_counter()
    kernel_interp(x[:n_calls])
    t_interp_calls = perf_counter() - t0

    speedup = t_exact / t_interp_calls
    print(f"Interpolator setup time : {t_setup * 1e3:.1f} ms")
    print(f"Exact kernel   ({n_calls} calls): {t_exact * 1e3:.1f} ms")
    print(f"Interp kernel  ({n_calls} calls): {t_interp_calls * 1e3:.1f} ms")
    print(f"Speedup factor : {speedup:.1f}×")





.. rst-class:: sphx-glr-script-out

 .. code-block:: none

      0% |          | 🦖 0/2000 [00:00<?, ?it/s]      8% |▊         | 🦖 150/2000 [00:00<00:01, 1498.02it/s]     16% |█▌        | 🦖 316/2000 [00:00<00:01, 1591.14it/s]     25% |██▌       | 🦖 506/2000 [00:00<00:00, 1728.95it/s]     36% |███▋      | 🦖 729/2000 [00:00<00:00, 1924.86it/s]     51% |█████▏    | 🦖 1025/2000 [00:00<00:00, 2297.15it/s]     81% |████████  | 🦖 1614/2000 [00:00<00:00, 3516.93it/s]    100% |██████████| 🦖 2000/2000 [00:00<00:00, 3225.42it/s]
      0% |          | 🦖 0/1000 [00:00<?, ?it/s]     20% |█▉        | 🦖 195/1000 [00:00<00:00, 1944.87it/s]     49% |████▉     | 🦖 490/1000 [00:00<00:00, 2535.44it/s]    100% |██████████| 🦖 1000/1000 [00:00<00:00, 3926.89it/s]
      0% |          | 🦖 0/500 [00:00<?, ?it/s]     39% |███▉      | 🦖 194/500 [00:00<00:00, 1936.53it/s]     97% |█████████▋| 🦖 486/500 [00:00<00:00, 2510.95it/s]    100% |██████████| 🦖 500/500 [00:00<00:00, 2456.79it/s]
    Interpolator setup time : 620.7 ms
    Exact kernel   (500 calls): 203.8 ms
    Interp kernel  (500 calls): 0.1 ms
    Speedup factor : 1674.7×




.. GENERATED FROM PYTHON SOURCE LINES 176-182

Plot: Interpolation Relative Error
------------------------------------

The relative error of the interpolated kernel should remain well below
:math:`10^{-3}` across the range used for synchrotron population integrals.
Errors grow near the boundaries where the kernel values are extremely small.

.. GENERATED FROM PYTHON SOURCE LINES 182-203

.. code-block:: Python


    fig, axes = plt.subplots(2, 1, figsize=(8, 7), sharex=True)

    axes[0].loglog(x, F_ref, lw=2, label="Exact $F(x)$")
    axes[0].loglog(x, F_interp, ls="--", lw=1.5, label="Interpolated $F(x)$")
    axes[0].set_ylabel(r"$F(x)$")
    axes[0].legend()
    axes[0].grid(True, which="both", ls="--", alpha=0.3)
    axes[0].set_title("Interpolated vs Exact Synchrotron Kernel")
    axes[0].set_ylim([1e-6, 3])

    axes[1].loglog(x, rel_error, color="C2", lw=1.5)
    axes[1].axhline(1e-3, ls="--", color="gray", label="0.1% threshold")
    axes[1].set_xlabel(r"$x = \nu / \nu_c$")
    axes[1].set_ylabel(r"Relative error $|\Delta F| / F$")
    axes[1].legend()
    axes[1].grid(True, which="both", ls="--", alpha=0.3)
    axes[1].set_ylim([1e-7, 1e0])
    plt.tight_layout()
    plt.show()




.. image-sg:: /auto_examples/e_physics_synchro/images/sphx_glr_plot_synchrotron_kernels_002.png
   :alt: Interpolated vs Exact Synchrotron Kernel
   :srcset: /auto_examples/e_physics_synchro/images/sphx_glr_plot_synchrotron_kernels_002.png
   :class: sphx-glr-single-img





.. GENERATED FROM PYTHON SOURCE LINES 204-222

Discussion
----------

The synchrotron kernels :math:`F(x)` and :math:`G(x)` are the universal
building blocks of all single-electron synchrotron emissivity calculations.
Their asymptotic forms — :math:`F \propto x^{1/3}` at low :math:`x` and
:math:`F \propto \sqrt{x}\,e^{-x}` at high :math:`x` — directly produce the
spectral slopes observed in radio-to-X-ray synchrotron sources.

The interpolated kernel available via
:func:`~radiation.synchrotron.core.get_first_kernel_interpolator`
reproduces the exact values to better than 0.1% over the range
:math:`x \in [10^{-5}, 10^2]` while offering a substantial speed advantage
for population integrals. The setup cost (building the interpolation table)
is paid once, after which each evaluation is essentially free.

For population-level spectra, see the companion example
:ref:`sphx_glr_galleries_examples_e_physics_synchro_plot_PL_sed.py`.


.. rst-class:: sphx-glr-timing

   **Total running time of the script:** (0 minutes 2.537 seconds)


.. _sphx_glr_download_auto_examples_e_physics_synchro_plot_synchrotron_kernels.py:

.. only:: html

  .. container:: sphx-glr-footer sphx-glr-footer-example

    .. container:: sphx-glr-download sphx-glr-download-jupyter

      :download:`Download Jupyter notebook: plot_synchrotron_kernels.ipynb <plot_synchrotron_kernels.ipynb>`

    .. container:: sphx-glr-download sphx-glr-download-python

      :download:`Download Python source code: plot_synchrotron_kernels.py <plot_synchrotron_kernels.py>`

    .. container:: sphx-glr-download sphx-glr-download-zip

      :download:`Download zipped: plot_synchrotron_kernels.zip <plot_synchrotron_kernels.zip>`


.. only:: html

 .. rst-class:: sphx-glr-signature

    `Gallery generated by Sphinx-Gallery <https://sphinx-gallery.github.io>`_
